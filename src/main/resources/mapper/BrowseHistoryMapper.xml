<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.brandpromo.mapper.BrowseHistoryMapper">

    <insert id="upsert">
        INSERT INTO browse_history (user_id, product_id)
        VALUES (#{userId}, #{productId})
        ON CONFLICT (user_id, product_id) DO UPDATE SET created_at = NOW()
    </insert>

    <select id="findRecentProductIds" resultType="long">
        SELECT product_id FROM browse_history
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>
</mapper>
