<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.brandpromo.mapper.OperationLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log (user_id, action, target_type, target_id, ip_address)
        VALUES (#{userId}, #{action}, #{targetType}, #{targetId}, #{ipAddress})
    </insert>

    <select id="countByAction" resultType="map">
        SELECT action, COUNT(*) as count
        FROM operation_log
        WHERE created_at >= DATE_SUB(NOW(), INTERVAL #{days} DAY)
        GROUP BY action
        ORDER BY count DESC
    </select>
</mapper>
