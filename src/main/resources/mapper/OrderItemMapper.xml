<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.brandpromo.mapper.OrderItemMapper">

    <insert id="insertBatch">
        INSERT INTO order_item (order_id, product_id, product_name, product_image, price, quantity, subtotal)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.productImage},
             #{item.price}, #{item.quantity}, #{item.subtotal})
        </foreach>
    </insert>

    <select id="findByOrderId" resultType="com.brandpromo.entity.OrderItem">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

    <select id="findRecentProductIdsByUserId" resultType="long">
        SELECT DISTINCT oi.product_id
        FROM order_item oi
        JOIN orders o ON oi.order_id = o.id
        WHERE o.user_id = #{userId}
          AND o.status IN (1, 2, 3)
        ORDER BY o.created_at DESC
        LIMIT #{limit}
    </select>
</mapper>
