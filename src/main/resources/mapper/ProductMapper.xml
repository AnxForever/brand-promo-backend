<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.brandpromo.mapper.ProductMapper">

    <sql id="promoActiveCondition">
        p.promo_status = 1
        AND p.promo_price IS NOT NULL
        AND (p.promo_start_time IS NULL OR p.promo_start_time &lt;= NOW())
        AND (p.promo_end_time IS NULL OR p.promo_end_time &gt;= NOW())
    </sql>

    <sql id="effectivePriceExpr">
        CASE WHEN <include refid="promoActiveCondition" /> THEN p.promo_price ELSE p.price END
    </sql>

    <sql id="productSelectColumns">
        p.id,
        p.name,
        p.brand_id,
        b.name AS brand_name,
        p.category_id,
        p.category,
        <include refid="effectivePriceExpr" /> AS price,
        CASE WHEN <include refid="promoActiveCondition" /> THEN p.price ELSE p.original_price END AS original_price,
        p.price AS base_price,
        p.stock,
        p.description,
        p.image_url,
        p.images,
        p.specs,
        p.status,
        p.merchant_id,
        p.sales_count,
        p.view_count,
        p.promo_price,
        p.promo_start_time,
        p.promo_end_time,
        p.promo_status,
        CASE WHEN <include refid="promoActiveCondition" /> THEN 1 ELSE 0 END AS promo_active,
        p.created_at,
        p.updated_at
    </sql>

    <select id="findAll" resultType="com.brandpromo.entity.Product">
        SELECT <include refid="productSelectColumns" />
        FROM product p
        LEFT JOIN brand b ON p.brand_id = b.id
        <where>
            <if test="keyword != null and keyword != ''">
                AND (p.name LIKE CONCAT('%', #{keyword}, '%')
                     OR p.description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="category != null and category != ''">
                AND p.category = #{category}
            </if>
            <if test="merchantId != null">
                AND p.merchant_id = #{merchantId}
            </if>
        </where>
        <choose>
            <when test="sort == 'price_asc'">ORDER BY <include refid="effectivePriceExpr" /> ASC</when>
            <when test="sort == 'price_desc'">ORDER BY <include refid="effectivePriceExpr" /> DESC</when>
            <when test="sort == 'sales'">ORDER BY COALESCE(p.sales_count, 0) DESC</when>
            <when test="sort == 'newest'">ORDER BY p.created_at DESC</when>
            <otherwise>ORDER BY p.created_at DESC</otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM product
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%')
                     OR description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
            <if test="merchantId != null">
                AND merchant_id = #{merchantId}
            </if>
        </where>
    </select>

    <select id="findById" resultType="com.brandpromo.entity.Product">
        SELECT <include refid="productSelectColumns" />
        FROM product p
        LEFT JOIN brand b ON p.brand_id = b.id
        WHERE p.id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (
            name, brand_id, category_id, category, price, original_price, stock,
            description, image_url, images, specs, status, merchant_id,
            promo_price, promo_start_time, promo_end_time, promo_status
        )
        VALUES (
            #{name}, #{brandId}, #{categoryId}, #{category}, #{price}, #{originalPrice}, #{stock},
            #{description}, #{imageUrl}, #{images}, #{specs}, #{status}, #{merchantId},
            #{promoPrice}, #{promoStartTime}, #{promoEndTime}, #{promoStatus}
        )
    </insert>

    <update id="update">
        UPDATE product
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="brandId != null">brand_id = #{brandId},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="category != null">category = #{category},</if>
            <if test="price != null">price = #{price},</if>
            <if test="originalPrice != null">original_price = #{originalPrice},</if>
            <if test="stock != null">stock = #{stock},</if>
            <if test="description != null">description = #{description},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="images != null">images = #{images},</if>
            <if test="specs != null">specs = #{specs},</if>
            <if test="status != null">status = #{status},</if>
            <choose>
                <when test="promoStatus != null and promoStatus == 1">
                    promo_status = 1,
                    promo_price = #{promoPrice},
                    promo_start_time = #{promoStartTime},
                    promo_end_time = #{promoEndTime},
                </when>
                <when test="promoStatus != null">
                    promo_status = #{promoStatus},
                    promo_price = NULL,
                    promo_start_time = NULL,
                    promo_end_time = NULL,
                </when>
                <otherwise>
                    <if test="promoPrice != null">promo_price = #{promoPrice},</if>
                    <if test="promoStartTime != null">promo_start_time = #{promoStartTime},</if>
                    <if test="promoEndTime != null">promo_end_time = #{promoEndTime},</if>
                </otherwise>
            </choose>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM product WHERE id = #{id}
    </delete>

    <select id="findAllCategories" resultType="string">
        SELECT DISTINCT category FROM product WHERE category IS NOT NULL ORDER BY category
    </select>

    <update id="incrementViewCount">
        UPDATE product SET view_count = COALESCE(view_count, 0) + 1 WHERE id = #{id}
    </update>

    <select id="findPopular" resultType="com.brandpromo.entity.Product">
        SELECT <include refid="productSelectColumns" />
        FROM product p
        LEFT JOIN brand b ON p.brand_id = b.id
        WHERE p.status = 1
        ORDER BY (COALESCE(p.sales_count, 0) * 3 + COALESCE(p.view_count, 0)) DESC
        LIMIT #{limit}
    </select>

    <select id="findByCategoryIn" resultType="com.brandpromo.entity.Product">
        SELECT <include refid="productSelectColumns" />
        FROM product p
        LEFT JOIN brand b ON p.brand_id = b.id
        WHERE p.status = 1
          AND p.category IN
        <foreach collection="categories" item="cat" open="(" separator="," close=")">
            #{cat}
        </foreach>
        <if test="excludeIds != null and excludeIds.size() > 0">
            AND p.id NOT IN
            <foreach collection="excludeIds" item="eid" open="(" separator="," close=")">
                #{eid}
            </foreach>
        </if>
        ORDER BY COALESCE(p.sales_count, 0) DESC
        LIMIT #{limit}
    </select>

    <update id="decrementStock">
        UPDATE product
        SET stock = COALESCE(stock, 0) - #{quantity},
            sales_count = COALESCE(sales_count, 0) + #{quantity}
        WHERE id = #{id} AND COALESCE(stock, 0) >= #{quantity}
    </update>

    <select id="diagStockColumn" resultType="java.util.HashMap">
        SELECT column_name, data_type, column_default, is_updatable, generation_expression
        FROM information_schema.columns
        WHERE table_name = 'product' AND column_name = 'stock'
    </select>

    <select id="diagInsertStock" resultType="java.util.HashMap">
        INSERT INTO product (name, price, stock, status)
        VALUES ('__diag__', 1.00, #{stock}, 1)
        RETURNING id, stock
    </select>
</mapper>
